@model IEnumerable<MVCAccountantv2.Models.CashDisbursement>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CheckNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CashDisbursementDate)
        </th>
       @* <th>
            @Html.DisplayNameFor(model => model.CashAccountID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VendorID)
        </th>*@
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CheckNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CashDisbursementDate)
        </td>
       @*<td>
            @Html.DisplayFor(modelItem => item.CashAccount.BankName)
        </td>
       <td>
            @Html.DisplayFor(modelItem => item.EmployeeID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VendorID)
        </td>*@
        <td>
            <a asp-action="Edit" asp-route-id="@item.CheckNumber">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.CheckNumber">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.CheckNumber">Delete</a>
        </td>
    </tr>
}
</table>
